package app.freerouting.io.specctra.parser

/**
 * Former entry-point for saving and loading board rules. The public API has been
 * fully migrated to [app.freerouting.io.specctra.RulesWriter] and
 * [app.freerouting.io.specctra.RulesReader].
 *
 * <p>This class is retained as an empty shell to avoid breaking any binary
 * references until the next major release.
 */
class RulesFile private constructor()
